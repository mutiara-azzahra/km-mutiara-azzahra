const character = [
  "A",
  "A",
  "A",
  "B",
  "B",
  "A",
  "A",
  "B",
  "B",
  "A",
  "B",
  "A",
  "A",
  "B",
  "C",
];

const length = character.length;
const includes = character.includes("A");
const duplicates = input.reduce(function (acc, element, i, character) {});

console.log(length);
console.log(includes);

character.forEach(function (element, index) {
  console.log(element, index);
});

//method
//git
//build in function

// for in
